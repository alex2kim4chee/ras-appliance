#
# Copy this file to `.env.local` (ignored by git) and update the endpoint
# to point at your Cloudflare Worker URL. Example:
# VITE_SERVICE_REQUEST_ENDPOINT=https://ras-worker.your-subdomain.workers.dev/service-request
#

VITE_SERVICE_REQUEST_ENDPOINT=
